# 規格書閱讀教學：以 CLNS0_B007 訊息通知批次作業為例

> 這份教學的目標：讓你看到任何一份規格書，都能照著同樣的步驟拆解，最後在腦中組出「這支程式從頭到尾做了什麼事」的畫面。我們會用 `CLNS0_B007訊息通知批次作業_新版規格.md` 這份真實規格書逐段講解。

---

## 0. 先建立心態：規格書是「說明書」，不是「小說」

規格書不是拿來一行一行照抄成程式碼的，它是拿來回答幾個問題的：

1. 這支程式**是什麼**？（叫什麼名字、放在哪個系統、什麼類型）
2. 這支程式**什麼時候跑、跑幾次**？
3. 這支程式**碰哪些資料表**？
4. 這支程式**從頭到尾的邏輯順序**是什麼？
5. 這支程式**跟外部系統怎麼溝通**（呼叫哪些 API）？

只要你能依序回答這 5 個問題，就代表你「看懂」這份規格書了。下面我們就照這個順序拆解。

---

## 1. 第一步：看目錄，先建立「地圖」

打開規格書先別急著看內文，先看目錄結構（第 5~63 行）。目錄的階層本身就是在告訴你這份文件的骨架：

| 章節 | 白話翻譯 |
| --- | --- |
| 壹、系統規格 | 這支程式的身分證（名字、路徑、類型） |
| 貳、需求描述 | 一句話講完「這是做什麼的」 |
| 參、需求類型 | 是畫面？服務？還是批次？多久執行一次？ |
| 肆、資料表 | 會用到哪些資料表，資料庫在哪 |
| 伍、處理邏輯說明 | **重點中的重點**：程式從開始到結束的每一步 |
| 陸、API 規格 | 跟外部系統溝通時，電文長什麼樣子 |

養成習慣：拿到規格書先掃過目錄，心裡對這 6 塊有個底，之後讀內文才不會迷路。

---

## 2. 拆解「壹、系統規格」—— 這支程式的身分證

對應原文第 74~80 行：

```
AP ID：LON-LX-LIP-01
程式名稱：CLNS0_B007
URI：/commonEntrance
類型：Spring Batch
運行平台：Paas 平台
```

白話解釋：

- **AP ID**：這支程式歸屬在哪個系統下面的代號，像是部門編號。
- **程式名稱 `CLNS0_B007`**：之後在資料庫（`CLN_BATCH_CTL`）裡查執行紀錄，就是用這個名字去查。
- **類型：Spring Batch**：這不是一個「使用者按按鈕才會動」的功能，而是**排程自動執行**的批次程式（Java 的 Spring Batch 框架）。
- **URI：/commonEntrance**：批次程式通常會有一個共用的進入點（entrance），外部排程系統（像是排程器）打這個路徑來觸發它。

再搭配「參、需求類型」第 87~88 行：

```
■批次
執行時間/頻率：自動：每 5 分鐘
```

結論：**這是一支每 5 分鐘自動跑一次的批次程式**，不是人手動操作的畫面或即時服務。這個資訊很關鍵，因為它決定了後面邏輯設計的方向——例如「怎麼避免同一支程式被同時啟動兩次」（防止重複執行）就是批次程式常見的必要邏輯。

---

## 3. 拆解「肆、資料表」—— 先認識這些資料表是誰

對應原文第 102~111 行，這支程式碰 4 張表：

| 資料表 | 白話角色 |
| --- | --- |
| `CLN_BATCH_CTL` | 批次的「打卡鐘」：記錄這支程式今天有沒有在跑、跑到哪、成功還是失敗 |
| `CLN_MESSAGE_LOG` | 訊息的「待辦事項＋近況」：最近 6 個月要發送 / 已發送的通知都在這 |
| `CLN_MESSAGE_HISTORY` | 訊息的「歸檔倉庫」：超過 6 個月的舊資料搬來這裡，`CLN_MESSAGE_LOG` 才不會越長越肥 |
| `CLN_MESSAGE_SETTING` | 通知的「設定總表」：定義有哪些通知模板、什麼時間該發送、多久發一次 |

**讀規格書的技巧**：先把每張表想成一個「角色」，之後在處理邏輯裡看到表名，直接代入角色去理解，會比死記欄位快很多。例如看到 `CLN_BATCH_CTL` 就想「喔，這是在打卡」；看到 `CLN_MESSAGE_SETTING` 就想「喔，這是去問設定總表今天要發什麼」。

---

## 4. 拆解「伍、處理邏輯說明」—— 核心中的核心

這段最長也最重要，我們用一個比喻來貫穿全場：**把這支程式想像成一位「送信員」，每 5 分鐘出勤一次**。

### 4.1 前置作業（原文 113~126 行）：先看今天有沒有人代班中

```
select NVL(COUNT(1),0) as RUNNING
from CLN_BATCH_CTL
where EXEC_DATE = @startDate
  and JOB_NAME = @程式名稱
  and JOB_STATUS = 1
```

白話：送信員上班前，先問打卡鐘「我今天的班是不是已經有人（或我自己上一輪）還在跑？」（`JOB_STATUS = 1` 代表執行中）。如果是，就記一筆 log 說「正在執行中，停止」然後直接下班（結束程式）。

**這是批次程式的經典防呆設計**：因為每 5 分鐘就會被排程觸發一次，如果上一輪還沒跑完，這一輪絕對不能重疊跑，不然資料可能會被重複處理兩次。

### 4.2 執行批次前置（原文 127~154 行）：打卡上班

```
新增/更新 CLN_BATCH_CTL：
  EXEC_DATE = 今天
  JOB_NAME  = CLNS0_B007
  JOB_STATUS = '1'          -- 執行中
  JOB_START_TIME = 現在時間
  JOB_END_TIME = null       -- 還沒下班，先空著
```

白話：先查有沒有今天的打卡紀錄，沒有就新增、有就更新，把狀態改成「1（執行中）」，記錄開始時間。**這一步做完，前一步的防重複執行檢查才會生效**——因為下一輪 5 分鐘後進來時，會查到 `JOB_STATUS = 1` 而擋下來。

### 4.3 執行批次（原文 155~491 行）：正式出勤送信

這段最長，拆成幾個子步驟：

**① 決定今天要處理的參數**（原文 167~192 行）

- 如果沒有帶參數（`arg0` 是空的）→ 這是「自動排程」情境，用系統時間當日期/時間，種類是「全部」。
- 如果有帶參數 → 這是「手動補跑」情境（例如某天漏跑了，工程師手動指定日期、時間、種類重新執行）。

**白話**：程式要同時支援「自動每 5 分鐘跑」跟「人工手動補跑指定日期」兩種模式，用參數個數（0 / 1 / 2 / 3 個）去判斷是哪一種。

**② 清理舊資料**（原文 193~218 行）

```
把 6 個月以前、狀態已完成(NEWS_STATUS='3') 的 CLN_MESSAGE_LOG 資料
  → 先複製一份到 CLN_MESSAGE_HISTORY
  → 再從 CLN_MESSAGE_LOG 刪除
```

白話：送信員每次出勤，會順手把 6 個月前的舊信件記錄搬去倉庫歸檔，保持工作桌（`CLN_MESSAGE_LOG`）乾淨。

**③ 查詢今天要跑哪些「通知模板」**（原文 219~230 行）

```
select * from CLN_MESSAGE_SETTING
where SOURCE_KIND = '1'                          -- 批次來源
  and 今天 between STR_DATE and END_DATE          -- 在有效期間內
  [and SEND_TYPE = @SendType]                     -- 若有指定種類才加這條件
```

白話：去問設定總表「今天有效期內、屬於批次來源的通知模板有哪些？」——注意，這裡查到的是**模板清單**，不是最終要發的人；每個模板代表一種通知規則（例如：對保通知、繳款提醒⋯），下一步才會針對每個模板去抽出「實際要通知的人」。

**④ 判斷今天是不是假日**（原文 231~243 行）

程式會呼叫外部 API【LIP-B-COMBUSSDQ001 查詢營業日】，把日期格式轉換後傳過去，拿到 `WorkingDayFlag`（是否為工作日）反推 `isHoliday`。

白話：因為某些通知模板「假日不發送」，所以要先知道今天是不是假日，才能決定等下要不要略過某些模板。

**⑤ 逐一檢查每個模板，判斷現在這個時間點該不該執行**（原文 244~262 行）

這是全篇邏輯最燒腦的一段，拆解成人話：

```
對每一個通知模板 map：
  取出這個模板的：
    - 抽件開始時間 WRITE_STR_TIME
    - 抽件結束時間 WRITE_END_TIME
    - 假日是否要跑 WRITE_HOLIDAY
    - 執行週期 CYCLE_MIN（每幾分鐘跑一次）

  條件 1：如果今天是平日，或（今天是假日但這個模板設定「假日也要跑」）
  條件 2：現在時間有落在 [開始時間, 結束時間] 區間內
  條件 3：現在時間距離「開始時間」的分鐘數，除以週期取餘數，誤差在 1 分鐘內

  → 三個條件都成立，才真的執行這個模板的抽件與發送
```

**為什麼要這樣判斷？** 因為程式每 5 分鐘被排程叫醒一次，但不同的通知模板可能設定「每 30 分鐘跑一次」、「每小時跑一次」等不同週期。條件 3 的算法就是在檢查：**現在這個時間點，剛好對得上這個模板該執行的節拍**（用取餘數＋容許 1 分鐘誤差，是因為排程觸發時間不會分毫不差）。

打個比方：如果某個模板設定「從 09:00 開始，每 30 分鐘跑一次」，那麼 09:00、09:30、10:00⋯都應該觸發，但 09:05、09:10 這種不上不下的時間點就不該觸發——條件 3 就是在做這個篩選。

### 4.4 針對符合條件的模板，實際組訊息並發送（原文 263~491 行）

**① 抽件**：如果模板有設定 `EXTRACTSTR`（一段可被參數取代的 SQL），就把日期參數塞進去、執行 SQL，抽出實際要通知的對象清單 `extractstrList`（例如：這批貸款案件的承辦人、手機、email⋯）。

**② 逐筆組裝通知內容**（原文 278~348 行）：

```
對每一筆抽出來的資料：
  用模板的 MSG 內容，把裡面的 #欄位# 標記換成實際資料值 → 組出最終要發送的文字
  資料檢核：檢查必要欄位是否齊全，不合格就跳過這一筆（不中斷整批）
  依 SEND_TYPE 第一碼判斷通知方式：
    A → 簡訊（SMS）
    B → 內部信（行內 email）
    C → 外部信（目前實務上沒有這個情境）
```

白話：這一步就像是把「通知模板」＋「這個人的個資」合成一封客製化的信/簡訊，並依照類型分別丟進「簡訊清單」或「email 清單」裡等待批次發送。

**③ 每 200 筆分批寫入 LOG，狀態先設為「2：待發送」**（原文 350~400 行）

白話：先把「我準備要發這些通知」的紀錄寫進 `CLN_MESSAGE_LOG`，狀態標記為處理中，這樣萬一發送過程中程式意外中斷，資料庫也留有「這批本來要發但還沒確認結果」的紀錄可以追查。

**④ 呼叫外部發送 API**（原文 403~462 行）

- 簡訊清單不為空 → 呼叫【LIP-C-CMSSDSMST001 簡訊發送服務】
- Email 清單不為空 → 呼叫【LIP-C-CMSSMAILT001 行內信寄送】

呼叫回來後，**逐筆核對 API 回傳的結果**：哪幾筆失敗，就把該筆狀態改成 `statusFail`（'-1'），成功的維持原本設定的 `statusSuccess`（'3'）。

**⑤ 更新 LOG 最終狀態**（原文 463~484 行）

```
update CLN_MESSAGE_LOG
set NEWS_STATUS = 最終狀態(成功/失敗),
    SEND_COUNT = 發送次數,
    OUTPUT_TIME = 現在時間
where 依 APPLY_ID + SEND_TYPE + INFM_ID + INPUT_TIME (+ MEMO_KEY) 定位這一筆
```

白話：把③寫進去的「待發送」紀錄，依照④拿到的實際結果，更新成「成功」或「失敗」，並記下真正送出的時間。

**這一段的重點心法**：③→④→⑤ 其實就是「先立案 → 執行 → 回報結果」的三段式寫法，任何批次程式在「呼叫外部 API 有可能部分失敗」的情境下都很常見這種寫法，值得記住。

### 4.5 收尾三步（原文 492~526 行）

| 步驟 | 內容 | 白話 |
| --- | --- | --- |
| 紀錄執行結束時間 | `Log.info(...)` | 純粹寫 log，記下「幾點跑完」 |
| 例外處理 | `Log.fatal(...)`，`isSuccess = false` | 如果前面任何地方丟例外，這裡接住，標記「這次失敗了」 |
| FINALLY | 不管成功失敗都會執行 | 把 `CLN_BATCH_CTL` 的狀態改成 `9`（成功）或 `0`（失敗），寫下班時間 `JOB_END_TIME` |

白話：這就是送信員的「打卡下班」——不管今天送信過程有沒有出包，下班前一定要在打卡鐘上蓋章記錄「今天跑完了，結果是成功還是失敗」。這也呼應最開頭 4.1 的防重複執行檢查：如果沒有這個 FINALLY，`JOB_STATUS` 會一直卡在 `1`，下一輪就會誤判「還在執行中」而永遠無法再跑。

---

## 5. 拆解「陸、API 規格」—— 讀懂電文表格的方法

這份規格書用了 3 個外部 API，每個都用同樣的格式描述，抓到這個格式，看任何 API 規格都通用：

```
API URI：呼叫路徑
服務名稱：這支 API 做什麼
HTTP METHOD：通常是 POST
CONTENT-TYPE：application/json

上行電文（= Request，我方送出去的資料）
下行電文（= Response，對方回傳給我的資料）
```

電文表格的欄位意義：

| 欄位 | 意思 |
| --- | --- |
| LVL | 資料的巢狀層級（1 是最外層，2 是裡面一層，3 又更裡面⋯像資料夾的層數） |
| 欄位名稱 | JSON 裡的 key |
| 資料型態 / 最大長度 | 這個欄位要放什麼型態的資料、上限幾個字 |
| 必要 | 打 Y 代表這欄一定要有值 |
| 說明 | 這欄位的用途 |

例如「二、LIP-C-CMSSDSMST001 簡訊發送服務」的上行電文（原文 586~604 行），你可以這樣讀：

```
最外層 MWHEADER（電文共通標頭，所有電文都會有，放交易識別資訊）
  MSGID：電文代碼，固定字串 LIP-C-CMSSDSMST001
  SOURCECHANNEL：來源系統代號 LON-LX-LIP-01
  TXNSEQ：這筆交易的唯一序號（自己產生，確保不重複）

最外層 TRANRQ（交易請求本體，Transaction Request 的縮寫）
  ModuleTypeCode：模組代碼
  SMSNotifyList：陣列，每一筆是一則簡訊，裡面又有 CaseNbr、SMSNotifyContent...等欄位
```

**規格書裡的縮寫小提醒**：
- `MWHEADER` = Middleware Header（中介系統的共通標頭）
- `TRANRQ` = Transaction Request（上行/請求）
- `TRANRS` = Transaction Response（下行/回應）
- `RETURNCODE` / `RETURNDESC` = 回傳代碼／回傳說明，通常 `'0000'` 代表成功

下行電文的 `RETURNCODE` 判斷邏輯也值得注意（原文 621 行）：

```
0000 - 全部成功
9999 - 全部失敗
E023 - 這批裡面有部分成功、部分失敗
```

這跟前面 4.4 的「逐筆核對結果，各自更新狀態」是對應的——因為一次可能送出 200 筆，結果不會是全有或全無，而是要細看每一筆的 `ReturnDesc`。

---

## 6. 規格書的「暗語」對照表（之後看別份規格書也通用）

| 你會看到的寫法 | 白話意思 |
| --- | --- |
| `@變數名` | 代表一個變數／參數，等一下組 SQL 或呼叫 API 要代入它的實際值 |
| `Log.info(...)` | 記一般的執行紀錄 |
| `Log.fatal(...)` | 記嚴重錯誤紀錄，通常伴隨批次中止 |
| `Transaction.begin() / commit() / rollback()` | 資料庫交易的開始／確定寫入／出錯還原 |
| 上行電文 | 我方呼叫別人時「送出去」的資料（Request） |
| 下行電文 | 別人「回傳給我」的資料（Response） |
| LVL | 資料的巢狀層級 |
| NVL(x, 0) | Oracle SQL 語法：如果 x 是 null，就當作 0（避免空值造成判斷錯誤） |
| 批次控制檔 / `_CTL` 結尾的表 | 通常是拿來記錄「這支程式跑到哪、狀態如何」的表，不是業務資料本身 |
| JOB_STATUS 常見數值 | 常見約定：1=執行中、9=成功結束、0=失敗結束（實際值以規格書定義為準） |

---

## 7. 用一張流程圖總結整支程式

```
排程每 5 分鐘觸發
      │
      ▼
【前置作業】查 CLN_BATCH_CTL 是否已在執行中？
      │ 是 → 記 log，結束
      │ 否
      ▼
【執行批次前置】新增/更新 CLN_BATCH_CTL，狀態設為「執行中」
      │
      ▼
【執行批次】
  ① 判斷參數模式（自動 / 手動補跑）
  ② 搬移 6 個月前舊資料到 HISTORY 表
  ③ 查 CLN_MESSAGE_SETTING 取得今天有效的通知模板清單
  ④ 呼叫營業日 API，判斷今天是否假日
  ⑤ 逐一檢查每個模板：時間區間 + 週期節拍 + 假日規則 都吻合才繼續
        │ 符合
        ▼
     抽件 → 組訊息內容 → 分 SMS / Email 兩個清單
        │
        ▼
     每 200 筆寫入 CLN_MESSAGE_LOG（狀態＝待發送）
        │
        ▼
     呼叫簡訊 API / 呼叫 Email API
        │
        ▼
     依回傳結果，逐筆更新狀態為成功／失敗
      │
      ▼
【紀錄結束時間】
      │
      ▼
【例外處理】若中途出錯 → isSuccess = false
      │
      ▼
【FINALLY】不論成功失敗，都更新 CLN_BATCH_CTL 狀態（9成功／0失敗）＋下班時間
```

把這張圖背起來，之後看程式碼實作時，隨時可以拿它當地圖比對「我現在寫的這段程式，對應規格書的哪一塊」。

---

## 8. 自我檢查清單：你真的看懂了嗎？

讀完一份規格書，試著不看文件、自己回答這 5 題：

1. 這支程式叫什麼名字？多久跑一次？
2. 如果同一支程式的上一輪還沒跑完，這一輪會發生什麼事？
3. 資料表 `CLN_MESSAGE_LOG` 跟 `CLN_MESSAGE_HISTORY` 的差別是什麼？什麼情況資料會從前者搬到後者？
4. 一個通知模板要滿足哪三個條件，這一輪才會真的執行？
5. 發送簡訊/email 後，如果 API 回傳「部分失敗」，程式怎麼知道是「哪幾筆」失敗？

如果 5 題都答得出來，代表你已經完整消化了 CLNS0_B007 這份規格書，可以放心地照著它寫程式或 review 別人寫的程式了。

---

## 9. 下次看新規格書的行動步驟（複習卡）

1. 先掃目錄，抓出「系統規格 / 需求 / 資料表 / 處理邏輯 / API」5 大塊。
2. 看系統規格，確認：類型（畫面/服務/批次）、頻率、AP ID。
3. 看資料表，把每張表想成一個「角色」，記住它的用途。
4. 看處理邏輯，用「這是在做什麼比喻」的方式，把每個段落翻成一句白話（像本文的送信員比喻）。特別留意：防重複執行、交易 begin/commit 的範圍、迴圈裡的判斷條件、成功/失敗分流。
5. 看 API 規格，記住上行=Request、下行=Response，只看「必要欄位」跟「回傳代碼含義」，其餘照抄格式即可。
6. 最後畫一張像第 7 節那樣的流程圖，或用第 8 節的 5 題自我檢查。
